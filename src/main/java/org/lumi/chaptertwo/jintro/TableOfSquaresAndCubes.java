package org.lumi.chaptertwo.jintro;

/**
 * Created by John Tsantilis
 * (i [dot] tsantilis [at] yahoo [dot] com A.K.A lumi) on 12/2/2017.
 */

public class TableOfSquaresAndCubes {
    public static void main(String[] args) {
        int alpha = 0;

        System.out.printf("number  square  cube%n");
        System.out.printf("%d       %d       %d%n", alpha, alpha *alpha, alpha * alpha * alpha);
        alpha = alpha + 1;
        System.out.printf("%d       %d       %d%n", alpha, alpha *alpha, alpha * alpha * alpha);
        alpha = alpha + 1;
        System.out.printf("%d       %d       %d%n", alpha, alpha *alpha, alpha * alpha * alpha);
        alpha = alpha + 1;
        System.out.printf("%d       %d       %d%n", alpha, alpha *alpha, alpha * alpha * alpha);
        alpha = alpha + 1;
        System.out.printf("%d       %d      %d%n", alpha, alpha *alpha, alpha * alpha * alpha);
        alpha = alpha + 1;
        System.out.printf("%d       %d      %d%n", alpha, alpha *alpha, alpha * alpha * alpha);
        alpha = alpha + 1;
        System.out.printf("%d       %d      %d%n", alpha, alpha *alpha, alpha * alpha * alpha);
        alpha = alpha + 1;
        System.out.printf("%d       %d      %d%n", alpha, alpha *alpha, alpha * alpha * alpha);
        alpha = alpha + 1;
        System.out.printf("%d       %d      %d%n", alpha, alpha *alpha, alpha * alpha * alpha);
        alpha = alpha + 1;
        System.out.printf("%d       %d      %d%n", alpha, alpha *alpha, alpha * alpha * alpha);
        alpha = alpha + 1;
        System.out.printf("%d      %d     %d%n", alpha, alpha *alpha, alpha * alpha * alpha);

    }

}
